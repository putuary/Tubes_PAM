/**
 * @format
 */

import {
    AppRegistry
} from 'react-native';
import DaftarFilm from './src/DaftarFilm';
import {
    name as appName
} from './app.json';

AppRegistry.registerComponent(appName, () => DaftarFilm);